---
import MainLayout from '../../../layouts/MainLayout.astro';
import Header from '../../../components/Header.astro';
import Breadcrumbs from '../../../components/Breadcrumbs.astro';
import Footer from '../../../components/Footer.astro';
import AIChatbot from '../../../components/AIChatbot.astro';
import { ArrowRight, Calculator, BarChart3, Briefcase } from 'lucide-astro';

const lang = 'en';

const areas = [
  {
    icon: Calculator,
    title: 'Finance & Back Office',
    text: 'Supplier invoices, month-end close, reminders, and financial master data — documented in two client case studies.',
    href: '/en/industries/finance-teams/',
  },
  {
    icon: Briefcase,
    title: 'Customer Communication & Intake',
    text: 'Email triage, phone reception, WhatsApp, and quote requests — so customer work stops leaking into administration.',
    href: '/en/industries/professional-services/',
  },
  {
    icon: BarChart3,
    title: 'Reporting & Data',
    text: 'Power BI dashboards, Excel migration, and data quality — numbers you trust, refreshing themselves.',
    href: '/en/industries/reporting-data/',
  },
];
---

<MainLayout
  title="Where We Help - eFlury Consulting"
  description="The three business areas where we automate for Swiss SMEs: finance & back office, customer communication & intake, reporting & data — grounded in documented projects."
  keywords="AI automation industries, finance team automation, professional services AI, Swiss SME"
  lang={lang}
>
  <Header lang={lang} />

  <Breadcrumbs lang={lang} items={[{ label: 'Where We Help' }]} />

  <main class="industries-page">
    <section class="hub-hero">
      <div class="container">
        <span class="hero-badge">For Swiss SMEs in any industry</span>
        <h1>Where we help.</h1>
        <p class="lead">
          The same processes eat time in every industry: invoices arrive, emails pile up,
          reports get built by hand. These pages show what we automate in each business
          area — grounded in the projects and articles documented on this site.
        </p>
      </div>
    </section>

    <section class="hub-grid">
      <div class="container">
        <div class="card-grid">
          {areas.map((item) => (
            <a href={item.href} class="industry-card">
              <div class="card-icon"><item.icon size={22} /></div>
              <h2>{item.title}</h2>
              <p>{item.text}</p>
              <span class="card-link">See what we automate <ArrowRight size={14} /></span>
            </a>
          ))}
        </div>
        <p class="more-note">
          Looking for a specific capability instead? See the <a href="/en/services/">services overview</a>.
        </p>
      </div>
    </section>
  </main>

  <Footer lang={lang} />
  <AIChatbot />
</MainLayout>

<style>
  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hub-hero {
    padding: var(--spacing-3xl) 0 var(--spacing-2xl);
    background: linear-gradient(135deg, var(--primary-50) 0%, var(--secondary-50) 100%);
    border-bottom: 1px solid var(--border-color);
    text-align: center;
  }

  .hero-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(13, 148, 136, 0.1);
    color: var(--teal-text);
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }

  .hub-hero h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    color: var(--text-primary);
    margin-bottom: var(--spacing-md);
  }

  .hub-hero .lead {
    font-size: 1.25rem;
    line-height: 1.6;
    color: var(--text-secondary);
    max-width: 700px;
    margin: 0 auto;
  }

  .hub-grid {
    padding: var(--spacing-2xl) 0 var(--spacing-3xl);
    background: var(--bg-secondary);
  }

  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: var(--spacing-lg);
  }

  .industry-card {
    display: flex;
    flex-direction: column;
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
    padding: var(--spacing-xl);
    box-shadow: var(--shadow-sm);
    text-decoration: none;
    color: inherit;
    transition: all var(--transition-base);
  }

  .industry-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-md);
    border-color: var(--teal-text);
  }

  .card-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 46px;
    height: 46px;
    border-radius: var(--radius-md);
    background: rgba(13, 148, 136, 0.1);
    color: var(--teal-text);
    margin-bottom: var(--spacing-md);
  }

  .industry-card h2 {
    font-size: 1.35rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--spacing-sm);
  }

  .industry-card p {
    font-size: 0.98rem;
    line-height: 1.65;
    color: var(--text-secondary);
    margin-bottom: var(--spacing-md);
    flex-grow: 1;
  }

  .card-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--link-color);
    font-weight: 600;
    font-size: 0.9rem;
  }

  .more-note {
    margin-top: var(--spacing-xl);
    font-size: 0.9rem;
    color: var(--text-tertiary);
    text-align: center;
  }

  .more-note a {
    display: inline-flex;
    align-items: center;
    color: var(--link-color);
    text-decoration: none;
  }

  .more-note a:hover {
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .container {
      padding: 0 1.5rem;
    }

    .hub-hero {
      padding: var(--spacing-3xl) 0 var(--spacing-xl);
    }

    .hub-hero .lead {
      font-size: 1.05rem;
    }
  }
</style>
