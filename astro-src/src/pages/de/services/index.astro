---
import MainLayout from '../../../layouts/MainLayout.astro';
import Header from '../../../components/Header.astro';
import Breadcrumbs from '../../../components/Breadcrumbs.astro';
import Footer from '../../../components/Footer.astro';
import AIChatbot from '../../../components/AIChatbot.astro';
import { useTranslations } from '../../../i18n/utils';
import { ArrowRight, Cpu, Server, Calculator, BarChart3 } from 'lucide-astro';

const lang = 'de';
const t = useTranslations(lang);

const services = [
  {
    icon: Cpu,
    title: 'Claude Skills Entwicklung',
    description: 'Massgeschneiderte Claude Skills für Ihre spezifischen Workflows. Automatisieren Sie repetitive Aufgaben mit KI, die Ihr Geschäft versteht.',
    link: '/de/services/claude-skills',
    badge: 'Beliebteste',
    metric: '15-30 Std/Woche gespart'
  },
  {
    icon: Server,
    title: 'MCP Server Integration',
    description: 'Verbinden Sie Claude mit Ihren Geschäftssystemen über Model Context Protocol. Nahtlose Integration mit Datenbanken, APIs und Unternehmenssoftware.',
    link: '/de/services/mcp-integration',
    badge: null,
    metric: '5+ Systeme verbunden'
  },
  {
    icon: Calculator,
    title: 'Finanzprozess-Automatisierung',
    description: 'Automatisieren Sie Monatsabschluss, Reporting und Abstimmungen. KI-gestützte Finanzautomatisierung mit Bexio- und ABACUS-Integration.',
    link: '/de/services/finanzen-automatisierung',
    badge: null,
    metric: '40 Std/Monat gespart'
  },
  {
    icon: BarChart3,
    title: 'Power BI Beratung',
    description: 'Individuelle Power BI Dashboards und Reports für Schweizer Unternehmen. Von Datenmodellierung bis Executive Dashboards mit KI-Erkenntnissen.',
    link: '/de/services/power-bi',
    badge: null,
    metric: '15 Std/Woche gespart'
  }
];
---

<MainLayout
  title="Services | eflury.com"
  description={t('services_pages.index.description')}
  keywords="Claude Skills, MCP Integration, Finanzautomatisierung, Power BI Beratung, Schweizer KI-Services"
  lang={lang}
>
  <Header lang={lang} />

  <Breadcrumbs lang={lang} items={[{ label: 'Services' }]} />

  <main class="services-page">
    <section class="page-hero">
      <div class="container">
        <h1 class="page-title">{t('services_pages.index.title')}</h1>
        <p class="page-subtitle">{t('services_pages.index.subtitle')}</p>
        <p class="page-description">{t('services_pages.index.description')}</p>
      </div>
    </section>

    <section class="offer">
      <div class="container">
        <div class="services-grid">
          {services.map((service) => (
            <a href={service.link} class="service-card">
              <div class="card-top">
                <div class="icon-tile">
                  <service.icon size={28} />
                </div>
                {service.badge && <span class="popular-badge">{service.badge}</span>}
              </div>
              <h2>{service.title}</h2>
              <p>{service.description}</p>
              <div class="card-bottom">
                <span class="saving">{service.metric}</span>
                <span class="go">
                  <ArrowRight size={18} />
                </span>
              </div>
            </a>
          ))}
        </div>

        <h2 class="explore-title">Mehr entdecken</h2>
        <div class="explore-grid">
          <a href="/de/pricing" class="explore-card">
            <h3>Preise & Pakete</h3>
            <p>Transparente Festpreise — vom Micro-Projekt bis zum Enterprise-Paket.</p>
          </a>
          <a href="/de/skopaai" class="explore-card">
            <h3>SkopaAI <span class="badge-new">Neu</span></h3>
            <p>Vorkonfigurierter Mac mini mit KI-Assistent. Auspacken, anschliessen, loslegen.</p>
          </a>
          <a href="/de/case-studies" class="explore-card">
            <h3>Referenzen</h3>
            <p>Echte Projekte, messbare Resultate — inklusive der Taxed GmbH Fallstudie.</p>
          </a>
        </div>

        <section class="cta-section">
          <h2>Nicht sicher, welcher Service passt?</h2>
          <p>Buchen Sie ein kostenloses 30-Minuten-Strategiegespräch. Ich analysiere Ihre Workflows und empfehle den richtigen Ansatz.</p>
          <a href={`/${lang}/#contact`} class="cta-button">
            Kostenloses Strategiegespräch buchen
            <ArrowRight size={18} />
          </a>
        </section>
      </div>
    </section>
  </main>

  <Footer lang={lang} />
  <AIChatbot />
</MainLayout>

<style>
  /* Shared verbatim between en/ and de/ services index — edit both together. */
  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* --- Hero band ------------------------------------------------------ */
  .page-hero {
    position: relative;
    padding: 3.5rem 0 3rem;
    background: linear-gradient(180deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);
    overflow: hidden;
    text-align: center;
  }

  .page-hero::before {
    content: '';
    position: absolute;
    top: -140px;
    right: -100px;
    width: 420px;
    height: 420px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(13, 148, 136, 0.12), transparent 65%);
    pointer-events: none;
  }

  .breadcrumb {
    position: relative;
    font-size: 0.875rem;
    color: var(--text-tertiary);
    margin-bottom: 1.5rem;
  }

  .breadcrumb a {
    color: var(--text-secondary);
    text-decoration: none;
  }

  .breadcrumb a:hover {
    color: var(--eflury-teal);
  }

  .page-title {
    position: relative;
    font-size: clamp(2.25rem, 5vw, 3.25rem);
    font-weight: 800;
    letter-spacing: -0.025em;
    color: var(--text-primary);
    margin-bottom: 0.75rem;
  }

  .page-subtitle {
    position: relative;
    font-size: clamp(1.125rem, 2.5vw, 1.3125rem);
    color: var(--eflury-teal);
    font-weight: 650;
    margin-bottom: 0.75rem;
  }

  .page-description {
    position: relative;
    font-size: 1.0625rem;
    color: var(--text-secondary);
    max-width: 620px;
    margin: 0 auto;
    line-height: 1.65;
  }

  /* --- Service cards ----------------------------------------------------- */
  .offer {
    padding: 3.5rem 0 4.5rem;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .service-card {
    display: flex;
    flex-direction: column;
    padding: 2rem;
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 18px;
    text-decoration: none;
    transition: transform 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease;
  }

  .service-card:hover {
    transform: translateY(-4px);
    border-color: var(--eflury-teal);
    box-shadow: 0 12px 28px rgba(2, 37, 84, 0.1);
  }

  .card-top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    margin-bottom: 1.25rem;
  }

  .icon-tile {
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(13, 148, 136, 0.1);
    border-radius: 14px;
    color: var(--eflury-teal);
  }

  .popular-badge {
    padding: 0.3125rem 0.75rem;
    background: var(--eflury-teal);
    color: #fff;
    font-size: 0.6875rem;
    font-weight: 700;
    border-radius: 999px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .service-card h2 {
    font-size: 1.3125rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 0.625rem;
  }

  .service-card > p {
    font-size: 0.9625rem;
    color: var(--text-secondary);
    line-height: 1.65;
    flex: 1;
    margin: 0 0 1.5rem;
  }

  .card-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 1rem;
    border-top: 1px solid var(--border-color);
  }

  .saving {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--eflury-teal);
  }

  .go {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: var(--bg-secondary);
    border-radius: 50%;
    color: var(--text-secondary);
    transition: background 0.2s ease, color 0.2s ease, transform 0.2s ease;
  }

  .service-card:hover .go {
    background: var(--eflury-teal);
    color: #fff;
    transform: translateX(4px);
  }

  /* --- Explore row -------------------------------------------------------- */
  .explore-title {
    text-align: center;
    font-size: 1.375rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 3.5rem 0 0.5rem;
  }

  .explore-title::after {
    content: '';
    display: block;
    width: 40px;
    height: 3px;
    border-radius: 2px;
    background: var(--eflury-teal);
    margin: 0.75rem auto 0;
  }

  .explore-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
    margin-top: 1.75rem;
  }

  .explore-card {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
    padding: 1.375rem 1.5rem;
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: 14px;
    text-decoration: none;
    transition: transform 0.25s ease, border-color 0.25s ease;
  }

  .explore-card:hover {
    transform: translateY(-3px);
    border-color: var(--eflury-teal);
  }

  .explore-card h3 {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin: 0;
    font-size: 1.0625rem;
    color: var(--text-primary);
  }

  .badge-new {
    padding: 0.125rem 0.5rem;
    background: var(--eflury-teal);
    color: #fff;
    font-size: 0.625rem;
    font-weight: 700;
    border-radius: 999px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .explore-card p {
    margin: 0;
    font-size: 0.875rem;
    color: var(--text-secondary);
    line-height: 1.55;
  }

  /* --- CTA ------------------------------------------------------------------ */
  .cta-section {
    margin-top: 3.5rem;
    text-align: center;
    padding: 3.5rem 2rem;
    background: linear-gradient(135deg, var(--eflury-navy) 0%, var(--eflury-teal) 130%);
    border-radius: 20px;
  }

  .cta-section h2 {
    font-size: clamp(1.5rem, 3vw, 1.875rem);
    font-weight: 800;
    color: #fff;
    margin: 0 0 0.875rem;
  }

  .cta-section p {
    font-size: 1.0625rem;
    color: rgba(255, 255, 255, 0.85);
    margin: 0 auto 2rem;
    max-width: 520px;
    line-height: 1.6;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.9375rem 1.875rem;
    background: #fff;
    color: var(--eflury-navy);
    font-size: 1rem;
    font-weight: 600;
    border-radius: 12px;
    text-decoration: none;
    transition: background 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease;
  }

  .cta-button:hover {
    background: rgba(255, 255, 255, 0.92);
    transform: translateY(-1px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
  }

  /* --- Responsive -------------------------------------------------------------- */
  @media (max-width: 900px) {
    .explore-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .container {
      padding: 0 1.25rem;
    }

    .page-hero {
      padding: 2.75rem 0 2.25rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .offer {
      padding: 2.75rem 0 3.5rem;
    }

    .cta-section {
      padding: 2.75rem 1.5rem;
    }
  }
</style>
