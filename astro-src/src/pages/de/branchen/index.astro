---
import MainLayout from '../../../layouts/MainLayout.astro';
import IllustrationShowcase from '../../../components/IllustrationShowcase.astro';
import FloorPlanIllustration from '../../../components/illustrations/FloorPlanIllustration.astro';
import Header from '../../../components/Header.astro';
import Breadcrumbs from '../../../components/Breadcrumbs.astro';
import Footer from '../../../components/Footer.astro';
import AIChatbot from '../../../components/AIChatbot.astro';
import { ArrowRight, Calculator, BarChart3, Briefcase } from 'lucide-astro';

const lang = 'de';

const areas = [
  {
    icon: Calculator,
    title: 'Finanzen & Back Office',
    text: 'Kreditorenerfassung, Monatsabschluss, Mahnwesen und Finanz-Stammdaten — dokumentiert in zwei Kundenprojekten.',
    href: '/de/branchen/finanzteams/',
  },
  {
    icon: Briefcase,
    title: 'Kundenkommunikation & Annahme',
    text: 'E-Mail-Triage, Telefonannahme, WhatsApp und Offertanfragen — damit Kundenarbeit nicht in der Administration versickert.',
    href: '/de/branchen/dienstleister/',
  },
  {
    icon: BarChart3,
    title: 'Reporting & Daten',
    text: 'Power-BI-Dashboards, Excel-Migration und Datenqualität — Zahlen, denen Sie trauen, und die sich selbst aktualisieren.',
    href: '/de/branchen/reporting-daten/',
  },
];
---

<MainLayout
  title="Einsatzbereiche - eFlury Consulting"
  description="Die drei Geschäftsbereiche, in denen wir für Schweizer KMU automatisieren: Finanzen & Back Office, Kundenkommunikation & Annahme, Reporting & Daten — verankert in dokumentierten Projekten."
  keywords="KI-Automatisierung Branchen, Finanzteam Automatisierung, Dienstleister KI, Schweizer KMU"
  lang={lang}
>
  <Header lang={lang} />

  <Breadcrumbs lang={lang} items={[{ label: 'Einsatzbereiche' }]} />

  <main class="industries-page">
    <section class="hub-hero">
      <div class="container">
        <span class="hero-badge">Für Schweizer KMU jeder Branche</span>
        <h1>Einsatzbereiche.</h1>
        <p class="lead">
          Dieselben Prozesse fressen in jeder Branche Zeit: Rechnungen kommen, E-Mails stapeln
          sich, Reports werden von Hand gebaut. Diese Seiten zeigen, was wir pro Geschäftsbereich
          automatisieren — verankert in den Projekten und Artikeln auf dieser Website.
        </p>
      </div>
    </section>

    <IllustrationShowcase>
      <FloorPlanIllustration lang="de" />
    </IllustrationShowcase>

    <section class="hub-grid">
      <div class="container">
        <div class="card-grid">
          {areas.map((item) => (
            <a href={item.href} class="industry-card">
              <div class="card-icon"><item.icon size={22} /></div>
              <h2>{item.title}</h2>
              <p>{item.text}</p>
              <span class="card-link">Was wir automatisieren <ArrowRight size={14} /></span>
            </a>
          ))}
        </div>
        <p class="more-note">
          Sie suchen eine bestimmte Fähigkeit statt einer Branche? Zur <a href="/de/services/">Leistungsübersicht</a>.
        </p>
      </div>
    </section>
  </main>

  <Footer lang={lang} />
  <AIChatbot />
</MainLayout>

<style>
  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hub-hero {
    padding: var(--spacing-3xl) 0 var(--spacing-2xl);
    background: linear-gradient(135deg, var(--primary-50) 0%, var(--secondary-50) 100%);
    border-bottom: 1px solid var(--border-color);
    text-align: center;
  }

  .hero-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(13, 148, 136, 0.1);
    color: var(--teal-text);
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }

  .hub-hero h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    color: var(--text-primary);
    margin-bottom: var(--spacing-md);
  }

  .hub-hero .lead {
    font-size: 1.25rem;
    line-height: 1.6;
    color: var(--text-secondary);
    max-width: 700px;
    margin: 0 auto;
  }

  .hub-grid {
    padding: var(--spacing-2xl) 0 var(--spacing-3xl);
    background: var(--bg-secondary);
  }

  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: var(--spacing-lg);
  }

  .industry-card {
    display: flex;
    flex-direction: column;
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
    padding: var(--spacing-xl);
    box-shadow: var(--shadow-sm);
    text-decoration: none;
    color: inherit;
    transition: all var(--transition-base);
  }

  .industry-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-md);
    border-color: var(--teal-text);
  }

  .card-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 46px;
    height: 46px;
    border-radius: var(--radius-md);
    background: rgba(13, 148, 136, 0.1);
    color: var(--teal-text);
    margin-bottom: var(--spacing-md);
  }

  .industry-card h2 {
    font-size: 1.35rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--spacing-sm);
  }

  .industry-card p {
    font-size: 0.98rem;
    line-height: 1.65;
    color: var(--text-secondary);
    margin-bottom: var(--spacing-md);
    flex-grow: 1;
  }

  .card-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--link-color);
    font-weight: 600;
    font-size: 0.9rem;
  }

  .more-note {
    margin-top: var(--spacing-xl);
    font-size: 0.9rem;
    color: var(--text-tertiary);
    text-align: center;
  }

  .more-note a {
    display: inline-flex;
    align-items: center;
    color: var(--link-color);
    text-decoration: none;
  }

  .more-note a:hover {
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .container {
      padding: 0 1.5rem;
    }

    .hub-hero {
      padding: var(--spacing-3xl) 0 var(--spacing-xl);
    }

    .hub-hero .lead {
      font-size: 1.05rem;
    }
  }
</style>
